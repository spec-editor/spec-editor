# Tests for @implements("{{ id }}")
# {{ element.aspect }} / {{ element.element_type }}: {{ title }}
{% if content %}
# {{ content }}
{% endif %}

import pytest


def test_{{ title.lower().replace(' ', '_').replace('-', '_') }}():
    """{{ content or title }}"""
    # TODO: Arrange — set up test data
    # TODO: Act — call the function under test
    # TODO: Assert — verify the expected result
    pass
