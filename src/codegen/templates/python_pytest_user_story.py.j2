# @implements("{{ id }}")
# {{ element.aspect }} / {{ element.element_type }}: {{ title }}
{% if content %}# {{ content | replace('\n', '\n# ') }}
{% endif %}

import pytest


class Test{{ title.replace(' ', '') }}:
    """{{ content or title }}"""

{% set has_gwt = content and ('given' in content.lower() and 'when' in content.lower() and 'then' in content.lower()) %}

    def test_{{ title.lower().replace(' ', '_') }}_scenario(self):
        """{{ title }} — happy path scenario."""
{% if content %}
        # {{ content | replace('\n', '\n        # ') }}
{% endif %}

        # TODO: Implement test based on acceptance criteria
        pass

{% if has_gwt %}
    def test_{{ title.lower().replace(' ', '_') }}_acceptance(self):
        """Verify acceptance criteria for {{ title }}."""
        # Arrange: set up preconditions from Given
        # Act: perform the action from When
        # Assert: verify the outcome from Then

        # {{ content | replace('\n', '\n        # ') }}
        pass
{% endif %}

    def test_{{ title.lower().replace(' ', '_') }}_edge_cases(self):
        """{{ title }} — edge cases and error handling."""
        # TODO: Add edge case tests:
        # - Invalid input
        # - Missing required fields
        # - Boundary values
        pass
